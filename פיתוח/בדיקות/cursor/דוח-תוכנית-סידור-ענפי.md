# דוח תוכנית — סידור ענפי הפרויקט

**תאריך:** 26/06/2026  
**סטטוס:** תוכנית בלבד — **טרם בוצע**  
**שורש:** `C:\Users\dfusb\Documents\מכונת מכתבים\`

---

## 1. מטרה

לסדר את התיקייה הראשית כך שברור מיד מהי **הגרסה האחרונה לשימוש/העברה**, וכל השאר מרוכז תחת ענף **`פיתוח/`**.

**עקרונות:**
- לא למחוק שום קובץ
- לא לבנות build חדש
- לא לשנות קוד
- לא לשנות את תוכן V1.2 Portable או את ה-ZIP שלו
- רק להעביר קבצים וליצור README

---

## 2. מבנה שורש רצוי (אחרי ביצוע)

```
מכונת מכתבים/
├── LetterGenerator_V1.2_Portable/      ← גרסה אחרונה (לא נוגעים)
├── LetterGenerator_V1.2_Portable.zip   ← ZIP סופי (לא נוגעים)
├── README_גרסה_אחרונה.txt              ← ייווצר
├── פיתוח/                              ← כל השאר
├── .git/                               ← חריג טכני (נשאר בשורש)
└── .gitignore                          ← חריג טכני (נשאר בשורש)
```

> **הערה על `.git`:** קבצי Git נשארים בשורש כדי שלא תישבר היסטוריית ה-repo. הם מוסתרים בדרך כלל ב-Explorer ולא מפריעים לזיהוי הגרסה האחרונה.

---

## 3. מה נשאר בשורש — ללא שינוי

| פריט | פעולה |
|------|--------|
| `LetterGenerator_V1.2_Portable/` | **לא נוגעים** — רק אימות מבנה |
| `LetterGenerator_V1.2_Portable.zip` | **לא נוגעים** — רק אימות קיום וגודל |
| `.git/`, `.gitignore` | נשארים (תשתית Git) |

**אימות מבנה Portable (לפני ואחרי):**
```
LetterGenerator_V1.2_Portable/
├── app.exe
├── README.txt
├── templates/
│   ├── תחשיב זכויות אישי.docx
│   └── תחשיב זכויות אישי.json
└── output/
    └── README.txt
```

---

## 4. מבנה `פיתוח/` שייווצר

```
פיתוח/
├── קוד מקור/
├── דוחות/
├── בדיקות/
├── דוגמאות/
├── גרסאות ישנות/
├── לוגים/
└── זמני/
```

---

## 5. טבלת העברות — מה לאן

### 5.1 `פיתוח/קוד מקור/`

| מקור (שורש) | יעד |
|-------------|-----|
| `LetterGenerator/` | `פיתוח/קוד מקור/LetterGenerator/` |

**נשאר בתוך `LetterGenerator/`:** `src/`, `scripts/`, `templates/`, `release/`, `app.py`, `requirements.txt`, `*.spec` וכו'.

**יוצא מתוך `LetterGenerator/` לענפים אחרים** (ראו להלן).

---

### 5.2 `פיתוח/דוגמאות/`

| מקור | יעד |
|------|-----|
| `LetterGenerator/samples/` | `פיתוח/דוגמאות/samples/` |
| `LetterGenerator/output/` | `פיתוח/דוגמאות/lettergenerator_output/` |
| `תחשיב זכויות אישי/` | `פיתוח/דוגמאות/תחשיב זכויות אישי/` |

**תוכן `תחשיב זכויות אישי/`:** קבצי עיצוב/אפיון מקוריים (DOCX, JSON, Excel, PNG).

**תוכן `samples/`:** כל תיקיות הדוגמה (`pre_build_sample`, `rtl_one_page`, `editable_template_proof` וכו').

---

### 5.3 `פיתוח/בדיקות/`

| מקור (שורש) | יעד |
|-------------|-----|
| `cursor/` | `פיתוח/בדיקות/cursor/` |
| `Audit Folder With Spaces/` | `פיתוח/בדיקות/Audit Folder With Spaces/` |
| `בדיקת מחולל V1.2/` | `פיתוח/בדיקות/בדיקת מחולל V1.2/` |

**תוכן `cursor/`:** `build_tests/`, `perf_regression/`, `v1_2_audit/`, סקריפטי benchmark, לוגי בדיקות, תמונות preview, **וגם** קבצי `דוח-*.md` — יישארו בתוך `cursor/` (לא מפוצלים, כדי לא לשבור קישורים פנימיים).

---

### 5.4 `פיתוח/גרסאות ישנות/`

| מקור (שורש) | יעד | גודל משוער |
|-------------|-----|------------|
| `LetterGenerator_V1.1_Portable/` | `פיתוח/גרסאות ישנות/` | ~343 MB |
| `LetterGenerator_V1.1_Portable.zip` | `פיתוח/גרסאות ישנות/` | ~340 MB |
| `LetterGenerator_release/` | `פיתוח/גרסאות ישנות/` | ~343 MB |
| `LetterGenerator_release.zip` | `פיתוח/גרסאות ישנות/` | ~331 MB |

---

### 5.5 `פיתוח/דוחות/`

| מקור (שורש) | יעד |
|-------------|-----|
| `README.md` | `פיתוח/דוחות/README.md` |
| `עידכונים מכונת מכתבים.txt` | `פיתוח/דוחות/` |
| `מסמך אפיון – מחולל מכתבים אוטומטי (.txt` | `פיתוח/דוחות/` |
| *(לאחר ביצוע)* | `דוח-סידור-ענפי-פרויקט.md` — דוח סיום |

---

### 5.6 `פיתוח/לוגים/`

| מקור | יעד |
|------|-----|
| `LetterGenerator/poc_log.txt` | `פיתוח/לוגים/poc_log.txt` |
| `LetterGenerator/test_results.log` | `פיתוח/לוגים/test_results.log` |

לוגים בתוך `cursor/` (למשל `v1_2_build_test.log`) — נשארים ב-`פיתוח/בדיקות/cursor/`.

---

### 5.7 `פיתוח/זמני/`

| מקור | יעד |
|------|-----|
| `LetterGenerator/build/` | `פיתוח/זמני/LetterGenerator_build/` |
| `LetterGenerator/dist/` | `פיתוח/זמני/LetterGenerator_dist/` |
| `פיתוח/_organize_project.py` | `פיתוח/זמני/_organize_project.py` (אחרי הרצה) |

---

## 6. סיכום פריטים בשורש היום (12 פריטים להעברה)

| # | פריט | יעד |
|---|------|-----|
| 1 | `LetterGenerator/` | קוד מקור (+ פיצול משנה) |
| 2 | `cursor/` | בדיקות |
| 3 | `Audit Folder With Spaces/` | בדיקות |
| 4 | `בדיקת מחולל V1.2/` | בדיקות |
| 5 | `תחשיב זכויות אישי/` | דוגמאות |
| 6 | `LetterGenerator_V1.1_Portable/` | גרסאות ישנות |
| 7 | `LetterGenerator_V1.1_Portable.zip` | גרסאות ישנות |
| 8 | `LetterGenerator_release/` | גרסאות ישנות |
| 9 | `LetterGenerator_release.zip` | גרסאות ישנות |
| 10 | `README.md` | דוחות |
| 11 | `עידכונים מכונת מכתבים.txt` | דוחות |
| 12 | `מסמך אפיון – מחולל מכתבים אוטומטי (.txt` | דוחות |

**סה"כ נפח משוער להעברה:** ~3.5 GB (העתקה = העברה, לא כפילות).

---

## 7. קובץ שייווצר בשורש

**`README_גרסה_אחרונה.txt`** — תוכן מתוכנן:

```
גרסה אחרונה לשימוש:
LetterGenerator_V1.2_Portable

קובץ להעברה:
LetterGenerator_V1.2_Portable.zip

הפעלה:
1. לחלץ את ה-ZIP לתיקייה במחשב.
2. להפעיל app.exe.
3. קבצי התבנית נמצאים בתיקיית templates.
4. ניתן לערוך את קובץ Word:
   templates/תחשיב זכויות אישי.docx
   כדי לשנות מלל ועיצוב.
5. את קובץ JSON יש לערוך רק להגדרות טכניות.
6. הפלט נשמר בתיקיית output.
7. נדרש Microsoft Word מותקן לצורך יצירת PDF.

כל שאר הקבצים, הקוד, הדוחות, הבדיקות, הדוגמאות והגרסאות הישנות נמצאים תחת:
פיתוח/
```

---

## 8. שלבי ביצוע מתוכננים

| שלב | פעולה |
|-----|--------|
| 1 | יצירת 7 תת-תיקיות תחת `פיתוח/` |
| 2 | העברת `LetterGenerator/` → `פיתוח/קוד מקור/` |
| 3 | פיצול מתוך `LetterGenerator/`: samples, output, build, dist, לוגים |
| 4 | העברת גרסאות ישנות (4 פריטים) |
| 5 | העברת בדיקות (3 תיקיות) |
| 6 | העברת דוגמאות ודוחות משורש |
| 7 | יצירת `README_גרסה_אחרונה.txt` |
| 8 | אימות שורש — רק 4+2 פריטים (כולל .git) |
| 9 | הרצת `LetterGenerator_V1.2_Portable\app.exe check` |
| 10 | הפקת `פיתוח/דוחות/דוח-סידור-ענפי-פרויקט.md` |

**כלי ביצוע:** סקריפט `פיתוח/_organize_project.py` (כבר מוכן) + יצירה ידנית של README ודוח סיום.

---

## 9. בדיקות אחרי ביצוע

```
LetterGenerator_V1.2_Portable\app.exe check
```

**לוודא:**
- [ ] `app.exe` עובד
- [ ] `templates/` מזוהים בנתיב יחסי ל-exe
- [ ] אין תלות ב-`פיתוח/` להפעלת Portable
- [ ] ZIP קיים ולא שונה (אותו גודל / hash)
- [ ] `output/` ב-Portable נקייה (רק README)
- [ ] תבניות DOCX+JSON שלמות

---

## 10. מה לא יבוצע

- ❌ מחיקת קבצים
- ❌ build / PyInstaller חדש
- ❌ שינוי קוד מקור
- ❌ שינוי תוכן `LetterGenerator_V1.2_Portable/`
- ❌ שינוי / בנייה מחדש של `LetterGenerator_V1.2_Portable.zip`

---

## 11. חריגים / ספקים

| נושא | החלטה |
|------|--------|
| `.git` / `.gitignore` בשורש | נשארים — תשתית Git |
| דוחות בתוך `cursor/` | לא מפורקים — כל `cursor/` עובר כיחידה ל`בדיקות/` |
| `Audit Folder With Spaces` | בדיקות (תיקיית audit ישנה עם app.exe) |
| `בדיקת מחולל V1.2` | בדיקות (הרצת בדיקה ישנה) |
| קבצים לא צפויים בשורש אחרי סידור | יועברו ל`פיתוח/זמני/` + יירשמו בדוח סיום |

---

## 12. מצב נוכחי

| פריט | סטטוס |
|------|--------|
| תוכנית | מוכנה (מסמך זה) |
| סקריפט `_organize_project.py` | קיים ב-`פיתוח/` |
| העברות בפועל | **לא בוצעו** |
| `README_גרסה_אחרונה.txt` | לא נוצר |
| דוח סיום | לא נוצר |

---

## 13. אישור לפני ביצוע

לאחר אישורך, אבצע את השלבים בסעיף 8, אריץ את הבדיקות, ואפיק את `דוח-סידור-ענפי-פרויקט.md`.
